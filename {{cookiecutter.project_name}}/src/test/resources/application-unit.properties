# This ensures that each unit test instance is unique insofar as Kafka state goes. If you set this to something static, tests will fail due to state not being completely blown away in between test runs
spring.application.name=${random.uuid}